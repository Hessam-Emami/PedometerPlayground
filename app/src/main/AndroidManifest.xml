<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emami.pedometerplayground">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".daisy.DaisyDebugActivity">

        </activity>
        <activity android:name=".google_sample.GooglePedometerActivity">

        </activity>
        <activity android:name=".uwaterloo.lab3_201_04.WaterlooActivity">

        </activity>
        <activity android:name=".oxford.OxfordActivity">

        </activity>
        <activity android:name=".daisy.DaisyMainActivity">

        </activity>
        <activity android:name=".filtering.FilteringActivity">

        </activity>

        <service
            android:name=".main.PedometerForegroundService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>